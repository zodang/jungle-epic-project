using UnityEngine;

public class HanTest : MonoBehaviour
{

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("한글 테스트");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
